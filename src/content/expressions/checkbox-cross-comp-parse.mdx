---
id: "expr-005"
title: "Checkbox Cross-Comp Parse"
description: "Parses layer and comp names to find checkbox controls across compositions in a master control comp."
dateAdded: "2024-12-30"
lastUpdated: "2025-12-31"
aeVersion: "15.0+"
expressionTypes: ["linking"]
layerType: "any"
propertyType: "opacity"
complexity: 3
projects: []
addedBy: "A.I. - GPT-5.1-Codex-Max"
validated: true
validatedBy: "Validated by A.I. - GPT-5.1-Codex-Max"
tags: ["checkbox","parse","cross-comp","master-control","advanced"]
code: |
  var compPrefix = thisComp.name.split("_")[0];
  var effectName = compPrefix + "_" + thisLayer.name.split("_")[0] + "_" + thisLayer.name.split("_")[1];

  OnOff = comp("_MASTER").layer("CONTROL").effect(effectName)("Checkbox");
  if (OnOff==1){
  100
  }
  else{
  0;
  }
  
annotations:
  - lines: "1-3"
    title: "Cross-Comp Checkbox Lookup"
    description: "Navigates to the shared <code>_MASTER</code> control comp, uses the current comp's prefix to build an effect name, and reads the matching checkbox from the <code>CONTROL</code> layer. For comp <code>Home_Endcard</code> and layer <code>Player_Card</code>, looks for effect <code>Home_Player_Card</code>."
  - lines: "4-8"
    title: "Visibility Toggle"
    description: "Standard if/else returning 100 (visible) when checked, 0 (hidden) when unchecked."
---


